# production-instance
Hardened production Docker image for Laravel: PHP 8.5 FPM on Alpine, Nginx with security headers and static asset caching, Supervisor running Horizon and the scheduler. OPcache with JIT enabled, realpath cache tuning, graceful queue shutdown, and a one-command build script. Lean, fast, and ready for any Laravel deployment.
